{
  "name": "chroma-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Local image palette extraction with K-means++ in OKLab.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 --bind 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check worker.js && node --check kmeans.mjs && node --check exports.mjs"
  },
  "engines": { "node": ">=22" }
}
